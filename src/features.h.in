#ifndef INCLUDE_features_h__
#define INCLUDE_features_h__

#define GIT_DEBUG_POOL ${GIT_DEBUG_POOL}
#define GIT_TRACE ${GIT_TRACE}
#define GIT_THREADS ${GIT_THREADS}
#define GIT_MSVC_CRTDBG ${GIT_MSVC_CRTDBG}

#define GIT_ARCH_64 ${GIT_ARCH_64}
#define GIT_ARCH_32 ${GIT_ARCH_32}

#define GIT_USE_ICONV ${GIT_USE_ICONV}
#define GIT_USE_NSEC ${GIT_USE_NSEC}
#define GIT_USE_STAT_MTIM ${GIT_USE_STAT_MTIM}
#define GIT_USE_STAT_MTIMESPEC ${GIT_USE_STAT_MTIMESPEC}
#define GIT_USE_STAT_MTIME_NSEC ${GIT_USE_STAT_MTIME_NSEC}
#define GIT_USE_FUTIMENS ${GIT_USE_FUTIMENS}
#define GIT_USE_REGCOMP_L ${GIT_USE_REGCOMP_L}

#define GIT_SSH ${GIT_SSH}
#define GIT_SSH_MEMORY_CREDENTIALS ${GIT_SSH_MEMORY_CREDENTIALS}

#define GIT_GSSAPI ${GIT_GSSAPI}
#define GIT_WINHTTP ${GIT_WINHTTP}

#define GIT_HTTPS ${GIT_HTTPS}
#define GIT_OPENSSL ${GIT_OPENSSL}
#define GIT_SECURE_TRANSPORT ${GIT_SECURE_TRANSPORT}
#define GIT_MBEDTLS ${GIT_MBEDTLS}

#define GIT_SHA1_COLLISIONDETECT ${GIT_SHA1_COLLISIONDETECT}
#define GIT_SHA1_WIN32 ${GIT_SHA1_WIN32}
#define GIT_SHA1_COMMON_CRYPTO ${GIT_SHA1_COMMON_CRYPTO}
#define GIT_SHA1_OPENSSL ${GIT_SHA1_OPENSSL}
#define GIT_SHA1_MBEDTLS ${GIT_SHA1_MBEDTLS}

#endif
